# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Build and test

on:
  pull_request:
    branches: [ "main","release/**","actions_test","fake-release" ]
    types: [opened, synchronize, reopened]

jobs:
  test:
    defaults:
      run:
        shell: pwsh
    if: github.event.pull_request.draft == false
    runs-on: windows-latest

    permissions:
      pull-requests: write  

    steps:
    
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          nuget-${{ runner.os }}-

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build OkapiLauncher/OkapiLauncher.csproj --configuration Release
      
    #- name: Test
    #  run: dotnet test OkapiLauncher.Tests.MSTest/OkapiLauncher.Tests.MSTest.csproj --verbosity normal
