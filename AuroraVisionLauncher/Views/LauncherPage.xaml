<Page
    x:Class="AuroraVisionLauncher.Views.LauncherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:AuroraVisionLauncher.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mahicon="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mod="clr-namespace:AuroraVisionLauncher.Models"
    xmlns:properties="clr-namespace:AuroraVisionLauncher.Properties"
    xmlns:vms="clr-namespace:AuroraVisionLauncher.ViewModels"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False,
                                     Type={x:Type vms:LauncherViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    AllowDrop="True"
    Style="{DynamicResource MahApps.Styles.Page}"
    mc:Ignorable="d">
    <Page.Resources>
        <conv:FileToIconConverter x:Key="FileIconConverter" />
        <conv:BoolToVisibilityConverter x:Key="BoolVis" />
        <CollectionViewSource
            x:Key="CustomViewSource"
            IsLiveGroupingRequested="True"
            Source="{Binding Apps}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Compatibility" />
            </CollectionViewSource.GroupDescriptions>
            <!--<CollectionViewSource.SortDescriptions>
                <componentModel:SortDescription PropertyName="YourPropertyForOrdering" />
            </CollectionViewSource.SortDescriptions>-->
        </CollectionViewSource>

    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="{StaticResource MediumLeftMargin}"
            Style="{StaticResource PageTitleStyle}"
            Text="{x:Static properties:Resources.MainPageTitle}" />
        <Grid
            Grid.Row="1"
            Margin="{StaticResource MediumLeftRightMargin}"
            Background="{DynamicResource MahApps.Brushes.Gray10}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Grid Margin="10,10,0,20" TextBlock.FontSize="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="{x:Static properties:Resources.ProgramDetailsProgramName}" TextAlignment="Right" />
                    <TextBlock Text="{x:Static properties:Resources.ProgramDetailsProjectType}" TextAlignment="Right" />
                    <TextBlock Text="{x:Static properties:Resources.ProgramDetailsSavedWithVersion}" TextAlignment="Right" />
                    <TextBlock Text="{x:Static properties:Resources.ProgramDetailsLocation}" TextAlignment="Right" />
                </StackPanel>
                <StackPanel Grid.Column="1" TextBlock.FontWeight="Bold">
                    <TextBlock Text="{Binding VisionProgram.Name, FallbackValue={x:Static properties:Resources.ProgramDetailsProgramNameFallback}}" />
                    <TextBlock Text="{Binding VisionProgram.Type, FallbackValue={x:Static properties:Resources.ProgramDetailsProjectTypeFallback}}" />
                    <TextBlock Text="{Binding VisionProgram.Version, FallbackValue={x:Static properties:Resources.ProgramDetailsSavedWithVersionFallback}}" />
                    <TextBlock Text="{Binding VisionProgram.Path, FallbackValue={x:Static properties:Resources.ProgramDetailsLocationFallback}}" />
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1">
                <ListView
                    ItemTemplate="{StaticResource AvAppFacadeDataTemplate}"
                    ItemsSource="{Binding Source={StaticResource CustomViewSource}}"
                    SelectedItem="{Binding SelectedApp, Mode=TwoWay}">
                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Text="{Binding Name}" />
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>
                </ListView>
            </Grid>
            <Button
                Grid.Row="2"
                Command="{Binding LaunchCommand}"
                TextBlock.FontSize="20">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Run with " />
                    <TextBlock Text="{Binding SelectedApp, TargetNullValue=...}" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
